<? if (!defined('B_PROLOG_INCLUDED') || B_PROLOG_INCLUDED !== true) die();
/** @var array $arResult */

/*if($arResult['USER_DATA']['USER_RECORD_DATA']){
    if($arResult['USER_DATA']['USER_RECORD_DATA']['PREF_CATEGORY']){
        if($arResult['PREF_CATEGORIES']){
            foreach ($arResult['PREF_CATEGORIES'] as $prefCategory){
                if($arResult['USER_DATA']['USER_RECORD_DATA']['PREF_CATEGORY'] == $prefCategory['ID']){
                    $arResult['USER_DATA']['PREF_CATEGORY'] = $prefCategory;
                }
            }
        }
    }
    if($arResult['USER_DATA']['USER_RECORD_DATA']['LOCATION']){
        if($arResult['USER_LOCATIONS']){
            foreach ($arResult['USER_LOCATIONS'] as $userLocation){
                if($arResult['USER_DATA']['USER_RECORD_DATA']['LOCATION'] == $userLocation['ID']){
                    $arResult['USER_DATA']['LOCATION'] = $userLocation;
                }
            }
        }
    }
}*/
